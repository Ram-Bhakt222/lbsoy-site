// LBSOY does not currently use Tailwind — global styles live in app/globals.css
// using the sage / warm-cream / charcoal / terracotta / gold palette set on :root.
// This file is kept as a stub for future migration. It is NOT loaded by anything
// in the current build (postcss.config.mjs has empty plugins, no @tailwind
// directives exist in CSS). Safe to delete or to repurpose.

const config = {
  content: [],
  theme: { extend: {} },
  plugins: [],
};

export default config;
