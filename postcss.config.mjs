// LBSOY does not use Tailwind or autoprefixer at the moment.
// This config is intentionally empty so Next.js falls back to its own defaults.
// If you decide to add Tailwind later, install `tailwindcss` + `autoprefixer`
// and put them back into the `plugins` object here.
export default {
  plugins: {},
};
